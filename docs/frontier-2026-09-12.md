100% DECODE COVERAGE ACHIEVED (2026-09-12)

The arm64jit decoder now handles EVERY instruction in the real libroblox.so
.text span [0x102d95980, 0x1072d5a84]: 11,437 initial Unsupported hits -> ZERO,
0 decode panics. This was the largest structural wall in the project; it is
permanently closed. 464 tests pass (incl. qemu-oracle diff battery).

Boot re-verified after decoder saturation. Idle barrier now precisely
characterized:
- All 3 guest threads (tids 0,1,2) park at the SAME guest call-site lr=0x10284d134
  inside a lamport-style per-thread futex barrier (wait fn @0x10284d018).
- Each waits on its OWN futex word at x19+4, awaiting "go" value 0xF4240.
- Probe `--futex-kick` writes its own free-running counter, which spuriously
  releases the futex; the guest re-arms (x19 -= 1) and re-parks. It is NOT a
  producer and cannot advance the engine.
- The engine's own producer thread is NOT among the 3 parked threads (all are
  awaits). The producer must be traced backwards from the latch initializers
  (0xF4240 sites at 0x24b11d0, 0x28546f0, 0x28f761c are the same barrier
  primitive used by the render/job queues).

NEXT LEVER — boot: find which thread initializes/feeds the per-thread futex
latches and what blocks the engine's producer from enqueueing the first unit of
work (a frame/render/job). Once a real unit is enqueued and a thread consumes
it, egl*/gl* hostcalls should begin firing through the Mesa llvmpipe wrappers.
Secondary: continue the GLES float bridge / glCompressedTexImage2D (ETC2/ASTC)
translation layer.

HEAD: 29fc56e (dev branch, local only; operator pushes).